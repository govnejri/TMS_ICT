# Руководство по внесению вклада

Спасибо за ваш интерес к проекту ICT Shipment Management System! 🎉

## Как внести вклад

### Сообщения об ошибках

Если вы нашли ошибку, пожалуйста:

1. Убедитесь, что ошибка еще не была зарегистрирована в Issues
2. Создайте новый Issue с подробным описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Скриншоты (если применимо)
   - Версия браузера и ОС

### Предложения новых функций

Мы открыты для новых идей! Создайте Issue с тегом "enhancement" и опишите:
- Что вы хотите добавить
- Почему это будет полезно
- Как это должно работать

### Pull Requests

1. **Fork репозитория**
   ```bash
   git clone https://github.com/ваш-username/ict.git
   cd ict
   ```

2. **Создайте ветку для вашей функции**
   ```bash
   git checkout -b feature/amazing-feature
   ```

3. **Установите зависимости**
   ```bash
   npm install
   ```

4. **Внесите изменения**
   - Следуйте существующему стилю кода
   - Добавьте комментарии где необходимо
   - Обновите документацию при необходимости

5. **Проверьте код**
   ```bash
   npm run lint
   npm run build
   ```

6. **Закоммитьте изменения**
   ```bash
   git add .
   git commit -m "feat: добавлена новая функция"
   ```

   Используйте семантические коммиты:
   - `feat:` - новая функция
   - `fix:` - исправление ошибки
   - `docs:` - изменения в документации
   - `style:` - форматирование кода
   - `refactor:` - рефакторинг
   - `test:` - добавление тестов
   - `chore:` - обслуживание

7. **Запушьте изменения**
   ```bash
   git push origin feature/amazing-feature
   ```

8. **Создайте Pull Request**
   - Опишите ваши изменения
   - Укажите связанные Issues (если есть)
   - Добавьте скриншоты (если применимо)

## Стиль кода

### TypeScript
- Используйте строгую типизацию
- Избегайте `any`
- Именуйте интерфейсы с большой буквы

### React
- Используйте функциональные компоненты
- Применяйте хуки правильно
- Деструктурируйте props

### CSS/Tailwind
- Используйте Tailwind утилиты
- Избегайте инлайн стилей
- Следуйте темной теме проекта

### Именование
- Компоненты: `PascalCase`
- Файлы компонентов: `PascalCase.tsx`
- Утилиты/хуки: `camelCase`
- Константы: `UPPER_SNAKE_CASE`

## Структура компонента

```tsx
import React from 'react';
import { Props } from './types';

interface ComponentProps {
  // Props definition
}

export const Component: React.FC<ComponentProps> = ({ prop1, prop2 }) => {
  // Hooks
  const [state, setState] = useState();

  // Effects
  useEffect(() => {
    // Effect logic
  }, []);

  // Handlers
  const handleClick = () => {
    // Handler logic
  };

  // Render
  return (
    <div>
      {/* JSX */}
    </div>
  );
};
```

## Тестирование

(В разработке - тесты будут добавлены позже)

## Вопросы?

Не стесняйтесь задавать вопросы в Issues или связаться с нами напрямую!

Спасибо за ваш вклад! ❤️
