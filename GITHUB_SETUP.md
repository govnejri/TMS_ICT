# 🚀 Быстрый старт: Загрузка проекта на GitHub

Это пошаговое руководство поможет вам загрузить проект ICT на GitHub.

## ✅ Предварительная проверка

Убедитесь, что у вас установлен:
- [ ] Git ([скачать](https://git-scm.com/))
- [ ] Аккаунт на GitHub ([регистрация](https://github.com/join))

## 📝 Шаг 1: Настройка Git (если еще не настроен)

```bash
# Настроить имя пользователя
git config --global user.name "Ваше Имя"

# Настроить email
git config --global user.email "ваш-email@example.com"

# Проверить настройки
git config --list
```

## 🌐 Шаг 2: Создание репозитория на GitHub

1. Откройте [github.com](https://github.com)
2. Нажмите "+" в правом верхнем углу → "New repository"
3. Заполните:
   - **Repository name**: `ict` (или любое другое)
   - **Description**: "ICT Shipment Management System"
   - **Visibility**: Public или Private
   - ⚠️ **НЕ** отмечайте "Initialize this repository with a README"
4. Нажмите "Create repository"

## 💻 Шаг 3: Инициализация Git в проекте

Откройте терминал в папке проекта (`d:\projects\ict`) и выполните:

```bash
# Инициализировать Git репозиторий
git init

# Добавить все файлы в staging
git add .

# Создать первый коммит
git commit -m "Initial commit: ICT Shipment Management System"
```

## 🔗 Шаг 4: Связать с GitHub

Замените `ваш-username` на ваш GitHub username:

```bash
# Добавить remote
git remote add origin https://github.com/ваш-username/ict.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Загрузить на GitHub
git push -u origin main
```

### Если используете SSH вместо HTTPS:

```bash
git remote add origin git@github.com:ваш-username/ict.git
git push -u origin main
```

## 🔐 Шаг 5: Настройка GitHub Secrets (для CI/CD)

1. Перейдите в ваш репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Нажмите "New repository secret"
4. Добавьте следующие секреты:

   - **Name**: `VITE_SUPABASE_URL`
     - **Value**: Ваш Supabase URL
   
   - **Name**: `VITE_SUPABASE_ANON_KEY`
     - **Value**: Ваш Supabase Anon Key
   
   - **Name**: `VITE_SHIPENGINE_API_KEY`
     - **Value**: Ваш ShipEngine API Key

## 📋 Шаг 6: Настройка описания репозитория

1. Перейдите на главную страницу репозитория
2. Нажмите ⚙️ "Settings" → "General"
3. В разделе "About" добавьте:
   - **Description**: "Современная система управления грузоперевозками с интерактивной картой"
   - **Website**: URL вашего deployed приложения (если есть)
   - **Topics**: `react`, `typescript`, `vite`, `tailwindcss`, `leaflet`, `supabase`, `shipment-tracking`

## 🏷️ Шаг 7: Создание первого релиза (опционально)

```bash
# Создать тег для версии 1.0.0
git tag -a v1.0.0 -m "Release version 1.0.0"

# Загрузить тег на GitHub
git push origin v1.0.0
```

Затем на GitHub:
1. Releases → "Create a new release"
2. Выберите тег `v1.0.0`
3. Заполните описание релиза
4. Нажмите "Publish release"

## 🔄 Последующие обновления

После внесения изменений в код:

```bash
# Проверить статус
git status

# Добавить измененные файлы
git add .

# Создать коммит с описанием
git commit -m "feat: добавлена новая функция"

# Загрузить на GitHub
git push
```

### Полезные Git команды

```bash
# Посмотреть историю коммитов
git log --oneline

# Посмотреть текущую ветку
git branch

# Создать новую ветку
git checkout -b feature/new-feature

# Переключиться на другую ветку
git checkout main

# Посмотреть изменения
git diff

# Отменить изменения в файле
git checkout -- filename

# Посмотреть remote URL
git remote -v
```

## 🎉 Готово!

Ваш проект теперь на GitHub! 

### Что дальше?

- ✅ Добавьте бейдж статуса CI в README
- ✅ Настройте GitHub Pages или Vercel для автодеплоя
- ✅ Пригласите коллаборантов
- ✅ Создайте первый Issue или Project Board
- ✅ Настройте Branch Protection Rules

### Полезные ссылки

- 📚 [Git Documentation](https://git-scm.com/doc)
- 📘 [GitHub Guides](https://guides.github.com/)
- 🎓 [GitHub Learning Lab](https://lab.github.com/)

---

## ⚠️ Важно!

**НИКОГДА** не коммитьте:
- ❌ `.env` файлы с секретами
- ❌ `node_modules/`
- ❌ API ключи
- ❌ Пароли

Эти файлы уже в `.gitignore`, но будьте внимательны!

---

## 🐛 Проблемы?

### Ошибка: "remote origin already exists"

```bash
# Удалить существующий remote
git remote remove origin

# Добавить заново
git remote add origin https://github.com/ваш-username/ict.git
```

### Ошибка: "failed to push some refs"

```bash
# Сначала получить изменения с GitHub
git pull origin main --rebase

# Затем загрузить
git push origin main
```

### Ошибка аутентификации

Если GitHub просит пароль, но пароль не работает:
1. Создайте [Personal Access Token](https://github.com/settings/tokens)
2. Используйте токен вместо пароля

Или настройте SSH ключи:
```bash
# Создать SSH ключ
ssh-keygen -t ed25519 -C "ваш-email@example.com"

# Добавить в ssh-agent
ssh-add ~/.ssh/id_ed25519

# Скопировать публичный ключ
cat ~/.ssh/id_ed25519.pub

# Добавить в GitHub: Settings → SSH and GPG keys
```

---

**Удачи с вашим проектом! 🚀**
