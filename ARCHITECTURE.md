# 🏗️ Архитектура проекта

## Обзор системы

```
┌─────────────────────────────────────────────────────────────┐
│                     ICT Shipment System                      │
│                   (React + TypeScript + Vite)                │
└─────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              │               │               │
              ▼               ▼               ▼
    ┌──────────────┐  ┌─────────────┐  ┌──────────────┐
    │  Shipments   │  │   Create    │  │   Map View   │
    │   Overview   │  │  Shipment   │  │   Tracking   │
    └──────────────┘  └─────────────┘  └──────────────┘
              │               │               │
              └───────────────┼───────────────┘
                              │
                     ┌────────▼─────────┐
                     │   API Services   │
                     └──────────────────┘
                              │
              ┌───────────────┼───────────────┐
              │               │               │
              ▼               ▼               ▼
      ┌─────────────┐  ┌──────────┐  ┌─────────────┐
      │  Supabase   │  │ShipEngine│  │  Nominatim  │
      │  Database   │  │   API    │  │     API     │
      └─────────────┘  └──────────┘  └─────────────┘
```

## Компонентная архитектура

```
App.tsx
├── ShipmentsOverview
│   ├── Фильтры статусов
│   ├── Список грузов
│   └── Кнопка создания
│
├── CreateShipmentFlow
│   ├── Шаг 1: CitySearch (origin)
│   ├── Шаг 2: CitySearch (destination)
│   ├── Шаг 3: Описание груза
│   ├── Шаг 4: Выбор перевозчика
│   └── Шаг 5: Подтверждение
│
└── MapView
    ├── Leaflet Map
    ├── Markers (origin/destination)
    └── Route Line
```

## Поток данных

### Создание груза

```
User Input
    │
    ▼
CitySearch ──► Nominatim API ──► Координаты
    │
    ▼
CreateShipmentFlow ──► ShipEngine API ──► Тарифы
    │
    ▼
apiService.createShipment() ──► Supabase ──► Сохранение
    │
    ▼
ShipmentsOverview ──► Обновление списка
```

### Просмотр груза на карте

```
ShipmentsOverview
    │
    │ (click track)
    │
    ▼
MapView
    │
    ├──► Render origin marker
    ├──► Render destination marker
    └──► Draw route line
```

## Структура базы данных

### Таблица: shipments

```sql
┌─────────────────┬──────────┬─────────────────────────┐
│     Column      │   Type   │      Description        │
├─────────────────┼──────────┼─────────────────────────┤
│ id              │ UUID     │ Primary Key             │
│ origin          │ TEXT     │ Город отправления       │
│ destination     │ TEXT     │ Город назначения        │
│ status          │ TEXT     │ Booked/In Transit/...   │
│ carrier         │ TEXT     │ Название перевозчика    │
│ goods_info      │ TEXT     │ Описание груза          │
│ price           │ DECIMAL  │ Стоимость доставки      │
│ current_location│ JSONB    │ {lat, lon}              │
│ estimated_arrival│TIMESTAMP│ Ожидаемое прибытие      │
│ progress        │ INTEGER  │ Процент выполнения      │
│ created_at      │ TIMESTAMP│ Дата создания           │
│ updated_at      │ TIMESTAMP│ Дата обновления         │
└─────────────────┴──────────┴─────────────────────────┘
```

## API Endpoints

### Внутренние (через apiService)

```
GET    /shipments              - Получить все грузы
GET    /shipments?status=...   - Фильтр по статусу
GET    /shipments/:id          - Получить конкретный груз
POST   /shipments              - Создать груз
PUT    /shipments/:id          - Обновить груз
DELETE /shipments/:id          - Удалить груз
```

### Внешние API

```
ShipEngine API:
POST   /v1/rates               - Получить тарифы

Nominatim API:
GET    /search                 - Поиск городов
       ?q={query}
       &format=json
       &limit=5
```

## Стек технологий

### Frontend
```
React 18.3
    ├── TypeScript 5.5
    ├── Vite 5.4
    ├── TailwindCSS 3.4
    ├── Lucide Icons
    └── React Leaflet 4.2
```

### Backend & Services
```
Supabase
    ├── PostgreSQL Database
    ├── Realtime Subscriptions
    └── Row Level Security

External APIs
    ├── ShipEngine (Rates)
    └── Nominatim (Geocoding)
```

### DevOps
```
GitHub
    ├── GitHub Actions (CI/CD)
    ├── GitHub Issues
    └── GitHub Pages (optional)

Deployment Options
    ├── Vercel
    ├── Netlify
    └── Docker
```

## Безопасность

### Уровни защиты

```
┌─────────────────────────────────────────┐
│         Frontend Application            │
│  - Environment variables (VITE_*)       │
│  - Client-side validation               │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│         Supabase Layer                  │
│  - Row Level Security (RLS)             │
│  - API Key authentication               │
│  - HTTPS encryption                     │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│         PostgreSQL Database             │
│  - Encrypted at rest                    │
│  - Automatic backups                    │
│  - Access logs                          │
└─────────────────────────────────────────┘
```

## Производительность

### Оптимизации

```
Build Time
    ├── Vite Fast Refresh (< 50ms HMR)
    ├── Code Splitting
    └── Tree Shaking

Runtime
    ├── React.memo для компонентов
    ├── useCallback для функций
    ├── useMemo для вычислений
    └── Debounce для поиска

Network
    ├── API Response Caching
    ├── Lazy Loading компонентов
    └── Image Optimization
```

## Мониторинг и отладка

```
Development
    ├── React DevTools
    ├── TypeScript Compiler
    ├── ESLint
    └── Browser Console

Production
    ├── Error Boundaries
    ├── Console Logging
    └── Performance Monitoring (future)
```

## Масштабируемость

### Горизонтальное масштабирование

```
Load Balancer
    │
    ├──► App Instance 1
    ├──► App Instance 2
    └──► App Instance 3
         │
         └──► Supabase (автоматическое масштабирование)
```

### Вертикальное масштабирование

```
- Увеличение ресурсов Supabase плана
- CDN для статических файлов
- Redis для кэширования (future)
- Read replicas для БД (future)
```

## Будущие улучшения

```
v2.0 (Планируется)
    ├── Authentication & Authorization
    ├── Real-time updates (WebSocket)
    ├── Push Notifications
    ├── Advanced Analytics
    ├── Mobile App (React Native)
    └── Multi-language Support

v3.0 (Концепт)
    ├── AI-powered route optimization
    ├── Blockchain tracking
    ├── IoT sensor integration
    └── Predictive analytics
```

---

📚 Дополнительная информация в [DOCS.md](DOCS.md)
